# notorm
save() events:
entityPreSave
entityPreCreate
entityCreate
entityPreUpdate
entityUpdate
entitySave

create() events:
entityPreCreate
entityCreate

update() events:
entityPreUpdate
entityUpdate

load() events:
entityLoad

first() events:
entityLoad

delete() events:
entityPreDelete (entityPredelete)
entityDelete

prepare() events:
entityPrepare

runSelect() events:
entityLoadMultiple

list() events:
entityLoadMultiple
